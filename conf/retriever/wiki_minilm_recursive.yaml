# Dense Retriever: MiniLM + Recursive Chunking
# Fast embedding model with smart chunking

type: dense
name: wiki_minilm_recursive
embedding_model: "all-MiniLM-L6-v2"
index_type: flat
artifact_path: wiki_minilm_recursive

chunking:
  strategy: recursive
  chunk_size: 512
  chunk_overlap: 50
