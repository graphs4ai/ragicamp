# @package _global_
# RAG Experiment - Fixed retrieval
# Usage: python -m ragicamp.cli.run experiment=rag

defaults:
  - override /model: gemma_2b_4bit
  - override /dataset: nq
  - override /agent: fixed_rag
  - override /retriever: dense
  - override /metrics: rag
  - override /evaluation: standard
  - override /judge: gpt4_mini
  - override /mlflow: default

# Experiment metadata
experiment:
  name: fixed_rag
  description: "Fixed RAG with dense retrieval"
  tags:
    - rag
    - dense_retrieval
