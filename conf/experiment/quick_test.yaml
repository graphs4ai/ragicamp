# @package _global_
# Quick Test Experiment
# Usage: python -m ragicamp.cli.run experiment=quick_test

defaults:
  - override /model: gemma_2b_4bit
  - override /dataset: nq
  - override /agent: direct_llm
  - override /metrics: fast
  - override /evaluation: quick
  - override /mlflow: disabled

# Experiment metadata
experiment:
  name: quick_test
  description: "Quick smoke test"
  tags:
    - test
    - quick
