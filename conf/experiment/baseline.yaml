# @package _global_
# Baseline Experiment - Direct LLM (no retrieval)
# Usage: python -m ragicamp.cli.run experiment=baseline

defaults:
  - override /model: gemma_2b_4bit
  - override /dataset: nq
  - override /agent: direct_llm
  - override /metrics: standard
  - override /evaluation: standard
  - override /judge: gpt4_mini
  - override /mlflow: default

# Experiment metadata
experiment:
  name: baseline_directllm
  description: "Baseline: Direct LLM without retrieval"
  tags:
    - baseline
    - directllm
